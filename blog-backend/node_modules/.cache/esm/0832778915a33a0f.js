let postsCtrl,checkLoggedIn;_15a‍.x([["default",()=>_15a‍.o]]);_15a‍.w("./posts.ctrl",[["*",null,function(v){postsCtrl=v}]]);_15a‍.w("../../lib/checkLoggedIn",[["default",["checkLoggedIn"],function(v){checkLoggedIn=v}]]);const Router = require("koa-router");



const posts = new Router();

posts.get("/", postsCtrl.list);
posts.post("/", checkLoggedIn, postsCtrl.write);

const post = new Router();

posts.get("/:id", postsCtrl.read);
posts.delete("/:id", checkLoggedIn, postsCtrl.remove);
posts.patch("/:id", checkLoggedIn, postsCtrl.update);

post.use("/:id", postsCtrl.getPostById, post.routes());

_15a‍.d(posts);
